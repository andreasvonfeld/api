# 📦 API E-Commerce — Node.js / Express / Prisma / PostgreSQL

Cette API e-commerce complète comprend :

- 🔐 Authentification (JWT)
- 👕 Produits (avec variantes / tailles)
- 🏷️ Catégories
- 🛒 Panier dynamique avec gestion du stock
- 👤 Gestion des utilisateurs (Admin)
- 📚 Documentation Swagger intégrée
- 🗄️ Prisma ORM avec PostgreSQL

---

## 📁 Structure du projet

API/
├── server.js
├── package.json
├── .env
├── swagger.js
├── prisma/
│ ├── schema.prisma
│ └── migrations/
│
├── config/
│ └── prismaClient.js
│
├── controllers/
│ ├── authController.js
│ ├── usersController.js
│ ├── productController.js
│ ├── categoryController.js
│ └── cartController.js
│
├── middleware/
│ ├── auth.js
│ └── admin.js
│
└── routes/
├── authRoutes.js
├── usersRoutes.js
├── productRoutes.js
├── categoryRoutes.js
└── cartRoutes.js

---

## ⚙️ Installation

### 1️⃣ Cloner le projet

```bash
git clone https://github.com/andreasvonfeld/api.git
cd api

## 2\. Configuration de MySQL et Prisma

### a. Configuration de la Base de Données

1.  **Créez un fichier `.env`** pour définir l'URI de connexion à votre base de données MySQL :

    ```
    # Remplacez les valeurs par les vôtres
    DATABASE_URL="mysql://root:@localhost:3306/products"
    PORT=5000
    JWT_SECRET=clesecret
    ```

2.  **Initialisez Prisma** :

    ```bash
    npx prisma init
    ```

    npx prisma migrate dev --name init

    npx prisma generate

    Cette commande crée le dossier `prisma` et le fichier `prisma/schema.prisma`. Elle configure automatiquement le `provider` à `mysql`.

    3. **lancer le serveur**

    npm start

    nodemon server.js


3. ##Lancement du serveur en local##

Installez la BDD dans le repo sur Wamp64 (local)

1.  Assurez-vous que votre serveur MySQL est démarré.
2.  Lancez l'application :
    ```bash
    node server.js
    ```
    (Ou `nodemon server.js` si vous l'avez installé)

Votre API CRUD est maintenant fonctionnelle avec **Express**, **Prisma**, et **MySQL** \! 🎉

4. ##Logins

👑 Admin par défaut pour tester
{
  "email": "test@test.com",
  "password": "azerty123"
}

👤 User classique pour tester
{
  "email": "user@test.com",
  "password": "azerty123"
}


